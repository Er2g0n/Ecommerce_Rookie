@model List<Structure_Core.ProductManagement.ProductsWithFirstImageDto>

@{ 
     ViewData["Title"] = "Motorcycle - HaiHa Harley"; 
 }
<section class="py-5">
    <div class="container">
        <h2 class="text-center fw-bold mb-5">LINE Harley-Davidson</h2>

        @if (Model == null || !Model.Any())
        {
            <p class="text-center">No motorcycles found.</p>
        }
        else
        {
            <div class="row g-4">
                @foreach (var product in Model)
                {
                    <div class="col-md-4">
                        <a href="/Product/Detail/@product.ProductCode" class="text-decoration-none text-dark">
                            <div class="card">
                                <img src="@product.FirstImagePath" class="card-img-top" alt="@product.ProductName">
                                <div class="card-body text-center">
                                    <h5 class="card-title">@product.ProductName</h5>
                                </div>
                            </div>
                        </a>
                    </div>
                }
            </div>
        }
    </div>
</section>